<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">
    
    <context:component-scan base-package="icu.sunnyc.rpc.demo.server"/>
    
    <context:property-placeholder location="classpath:simple-rpc.properties"/>

    <bean id="rpcServer" class="icu.sunnyc.rpc.core.server.RpcServer">
        <!-- RPC 服务启启动地址 -->
        <constructor-arg name="serviceAddress" value="${service.address}"/>
        <!-- 服务注册中心地址 -->
        <constructor-arg name="registryAddress" value="${registry.address}"/>
    </bean>

</beans>